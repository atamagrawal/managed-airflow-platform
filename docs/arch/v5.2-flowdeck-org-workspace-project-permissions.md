# Flowdeck — How Teams, Projects, and Permissions Work

> **Version:** 5.2  
> **Last Updated:** May 2026  
> **For:** Product managers, team leads, and anyone who needs to structure their organisation in Flowdeck or decide who can do what.  
> **Not for:** How the system is built internally — see v5.1 for that.

---

## Table of Contents

1. [Why this structure exists](#1-why-this-structure-exists)
2. [The four levels](#2-the-four-levels)
3. [Deciding how to structure your team](#3-deciding-how-to-structure-your-team)
4. [Who can do what — roles](#4-who-can-do-what--roles)
5. [Controlling who deploys to production](#5-controlling-who-deploys-to-production)
6. [Onboarding and offboarding people](#6-onboarding-and-offboarding-people)
7. [Automation and CI/CD pipelines](#7-automation-and-cicd-pipelines)
8. [Common scenarios](#8-common-scenarios)
9. [How Flowdeck compares to Astronomer Astro](#9-how-flowdeck-compares-to-astronomer-astro)

---

## 1. Why this structure exists

Flowdeck gives you a four-level hierarchy — **Organisation, Workspace, Project, Deployment** — because each level solves a different problem:

| Level | Problem it solves |
|-------|-------------------|
| **Organisation** | One billing account, one SSO config, one set of company-wide limits for your entire company. |
| **Workspace** | Different teams or environments should not see each other's pipelines. A workspace is the **hard line** between teams. |
| **Project** | One team often owns several related products or programmes. A project groups them without splitting into separate workspaces. |
| **Deployment** | One live Airflow environment. Teams typically have several — dev, staging, and production. |

The structure answers: **who can touch what, who pays for it, and what rules apply.**

---

## 2. The four levels

**Organisation → Workspace → Project → Deployment**

- One org has **many** workspaces.
- One workspace has **many** projects.
- One project has **many** deployments.
- Each deployment belongs to **exactly one** project, workspace, and org — it is never shared across two workspaces.

**Example:**

```
ORGANISATION: Acme Corp
│
├── WORKSPACE: Data engineering       ← team boundary
│     ├── PROJECT: Payments platform
│     │        ├── DEPLOYMENT: dev
│     │        ├── DEPLOYMENT: staging
│     │        └── DEPLOYMENT: production
│     └── PROJECT: Fraud detection
│              ├── DEPLOYMENT: staging
│              └── DEPLOYMENT: production
│
└── WORKSPACE: Analytics              ← another team, fully separate
       └── PROJECT: Reporting
                └── DEPLOYMENT: production
```

The two workspaces above are completely independent. A member of "Analytics" cannot see "Data engineering" environments, and vice versa — unless someone explicitly grants them access.

---

## 3. Deciding how to structure your team

This is the most important decision a PM makes in Flowdeck. Use these questions:

### Should I create a new workspace or a new project?

| Situation | Use a new… |
|-----------|------------|
| A different team who should **never** see our pipelines | **Workspace** — hard boundary, independent membership, own alert channels |
| Same team, but a distinct product or programme they own | **Project** — shared workspace settings and members, but own grouping and narrower invites |
| Just another environment (dev/staging/prod) | **Deployment** under the same project |

**Rule:** Workspace = team or org boundary. Project = product or programme boundary within a team.

### Can one workspace hold all our work?

Yes. Small organisations often use a single workspace with several projects — one per product. The workspace settings (who gets alerted, which Airflow versions are allowed, whether production requires automation) apply to everyone inside it.

### Can a deployment be shared across projects?

Structurally, a deployment always lives under **exactly one project**. It cannot be moved between projects or co-owned by two projects.

However, a project lead or workspace admin can grant teams from **other projects** a role on that deployment. This gives those teams access without changing ownership. The deployment still belongs to one project — the other projects are just permitted users.

**This is what "shared deployment" means in Flowdeck: one deployment, one owning project, multiple projects granted access.**

See [section 8](#8-common-scenarios) for when this makes sense and what the trade-offs are.

---

## 4. Who can do what — roles

Every person or automation account has a **role** at one or more levels. Higher-level roles automatically carry equivalent access to everything below them in the same branch.

### Roles at a glance

| Role | What this person can do |
|------|-------------------------|
| **Org owner** | Billing, SSO setup, all workspace admin rights everywhere, delete org. |
| **Org admin** | Create/remove workspaces, manage members and limits — but not billing. Full workspace admin everywhere. |
| **Org member** | Has an identity in your org but cannot touch anything until given a workspace/project role. |
| **Workspace admin** | Manages this workspace: members, production guardrails, defaults, alerts. Effectively a project lead in every project here. |
| **Workspace developer** | Creates and configures environments and projects. Cannot change workspace-wide settings or membership. |
| **Workspace viewer** | Read-only across this workspace. |
| **Project lead** | Manages this project: deployments, shared connections, signing keys, project members. Cannot touch other projects or workspace settings. |
| **Project developer** | Creates and changes deployments and builder-facing settings within this project. |
| **Project viewer** | Read-only for this project. |
| **Deployment deployer** | Ships new code **to this one environment**. The CI/CD pipeline role — cannot change how the environment is configured. |
| **Deployment operator** | Triggers runs, clears failed tasks, adjusts runtime settings. For on-call and analysts who run pipelines but don't ship code. |
| **Deployment viewer** | Sees run history and logs only. |

### Key rule: access only flows downward

An org admin is effectively an admin in every workspace. A workspace admin is effectively a project lead in every project. But a project developer cannot see other projects — and cannot see the workspace settings at all.

**Roles are additive.** A person who is "workspace viewer" overall but "project lead" on one project has read-only access everywhere else and full control in that project.

**There are no deny rules.** To keep someone out of a project, simply do not invite them to it.

### Who should get each role? (quick guide)

| Person | Suggested role |
|--------|---------------|
| Platform or infrastructure admin | Org admin |
| Team lead responsible for a workspace | Workspace admin |
| Data engineer building pipelines | Workspace or project developer |
| Analyst who runs reports but does not write code | Deployment operator |
| Security reviewer or exec stakeholder | Org member + workspace viewer |
| External contractor on one programme | Project developer on that project only |
| CI/CD pipeline | Deployment deployer (scoped key) |

---

## 5. Controlling who deploys to production

A workspace can be set to **require all code changes to go through an automated pipeline** rather than allowing engineers to push manually.

When this is on for a workspace:

- **CI/CD automation** (service accounts with a deployer key) can still ship code.
- **Human users** — even workspace admins — cannot push pipeline code directly. They can still trigger runs, read logs, and clear tasks.
- **Platform admins** can still change environment-level settings (like Airflow version or sizing), but that is separate from shipping DAG code.

**This is the main governance switch for production.** Turning it on means production only changes when your review process approves it. Turning it off means anyone with developer access can push directly.

---

## 6. Onboarding and offboarding people

### Inviting someone

- For **full team access**: invite them to the workspace with the right role.
- For **one programme only**: invite them directly to the project — they get the **Workspace Accessor** level automatically (minimal workspace visibility) and see only the deployments you assign.
- For **one environment only**: give them a deployment-level role directly.

### SSO and directory groups

If your company uses Okta, Microsoft Entra, or another identity provider, IT can map directory groups to Flowdeck roles. When a user joins or leaves a group, their access updates automatically on next sign-in — no manual Flowdeck steps required.

Example mapping:

| Company directory group | Flowdeck role |
|-------------------------|---------------|
| data-eng-all | Workspace developer — "Data engineering" |
| data-eng-leads | Workspace admin — "Data engineering" |
| payments-team | Project developer — "Payments platform" |
| audit-access | Workspace viewer — all production workspaces |

### When someone leaves

If directory sync is on: HR removes them from the IdP → their Flowdeck sessions and keys are disabled automatically, typically within minutes.

Without directory sync: an org admin removes them manually. The result is the same — sessions and keys stop working immediately. **Running environments are not affected** and keep running under automation.

---

## 7. Automation and CI/CD pipelines

Pipelines, scripts, and monitoring tools use **automation accounts** with **keys** instead of passwords. You assign them a role just like a person.

**Best practice: scope the key tightly.**

| Pipeline | Role | Key scope |
|----------|------|-----------|
| One pipeline per environment | Deployment deployer | That one deployment |
| One pipeline for dev/staging/prod in a programme | Project developer | That project |
| Read-only health check | Deployment viewer | That deployment |

A key scoped to a project cannot accidentally affect other workspaces even if the account's role would otherwise allow broader access.

Treat keys like passwords: store them in a secrets manager, rotate when team membership changes, revoke promptly when no longer needed.

---

## 8. Common scenarios

### Giving a contractor access to one programme

The project lead invites the contractor **directly to the project** with a developer role. The contractor:

- Can work on all deployments inside that project.
- **Cannot** see other projects in the workspace.
- **Cannot** change workspace-wide settings or membership.

If the workspace requires pipeline-only code pushes, the contractor must use a CI/CD key — not direct pushes.

### Two projects sharing one deployment

Sometimes two projects within the same workspace want to run DAGs on a single Airflow environment — typically to share infrastructure and avoid the cost of two separate deployments.

In Flowdeck, the deployment stays under **one owning project**. The other project's team is granted **Deployment deployer** access to it. The owning project's lead or workspace admin controls the environment config.

**The only supported pattern for cross-project sharing is DAG-only deploys:**

- Each project team uses a CI/CD pipeline with a **Deployment deployer** key scoped to that deployment.
- The deployer role allows shipping DAG code to the environment but **cannot** change environment configuration (Airflow version, sizing, connections, environment variables, resource limits).
- One owner (the project lead of the owning project, or a workspace admin) retains sole control over platform-level settings.
- Each project's pipeline pushes only its own DAG bundle independently.

If a team from another project also needs to change environment-level settings, they need their own deployment — not cross-project access.

**The shared deployment must carry all dependencies upfront:**

Because no project team can modify the environment after the fact (DAG-only deploy), the deployment must be configured with every package, provider, and dependency that any sharing project's DAGs will need — before those DAGs are deployed. This is a key responsibility of the deployment owner:

| Dependency type | Who sets it | When |
|-----------------|-------------|------|
| Python packages and Airflow providers | Deployment owner | Before any project team ships DAGs |
| Connections and variables shared across projects | Deployment owner (or workspace admin via project-level shared config) | Before DAGs reference them |
| Resource sizing (worker count, memory) | Deployment owner | Must account for the combined load of all projects |
| Airflow version | Deployment owner | Must be compatible with all projects' DAG code |

If a new project team needs a dependency that is not yet installed, they raise a request to the deployment owner — they cannot add it themselves. This coordination overhead is one of the practical costs of sharing a deployment.

**Trade-offs that remain even with DAG-only deploys:**

| Risk | What it means in practice |
|------|--------------------------|
| **Shared outage** | A runaway job or bad DAG from one project slows down the scheduler and workers for all projects sharing that deployment |
| **Shared version lock** | All projects run on the same Airflow version and sizing — a version upgrade requested by one project requires everyone to agree |
| **Unclear ownership** | The deployment lives under one project, but other projects are affected; agree upfront who is on-call and who approves platform changes |
| **Namespace clashes** | DAG IDs, connection names, and variable names must not conflict across projects — teams need a naming convention |
| **Compliance** | Regulators may require sensitive data pipelines to be isolated, even within the same workspace |

**When cross-project sharing makes sense:** teams in the same workspace sharing infrastructure for related programmes, low compliance risk, and all teams commit to DAG-only deploys with one clear platform owner.

**When each project should have its own deployment:** different release cadences, compliance or data isolation requirements, or any team that needs independent control over environment-level settings.

### Promoting an engineer to workspace admin

The current workspace admin changes their role. The promotion takes effect immediately. The change is recorded in the audit trail with who made it and when.

### Splitting a team into two workspaces

When two sub-teams need genuinely independent governance — separate alert channels, different production rules, or compliance reasons — they should be in separate workspaces. Once a deployment exists under a workspace, it cannot be "moved" to another workspace directly; the practical path is to create new deployments in the new workspace and retire the old ones.

### Adding a new product line inside an existing team

Add a new **project** inside the existing workspace. The team's workspace-wide membership, defaults, and alert routing all apply automatically. Invite anyone who needs narrower access directly at project level.

---

## 9. How Flowdeck compares to Astronomer Astro

[Astronomer Astro](https://www.astronomer.io/docs/astro/overview) is a widely used managed Airflow product. If your team is familiar with it, here is how the models map.

### Structure

Astro uses **Organization → Workspace → Deployment** — three levels, no Project. In Astro, what Flowdeck calls a "project" is typically handled by creating **another Workspace** or by naming conventions. Flowdeck adds Project as an optional fourth level so several programmes can live inside one workspace without merging all their deployments into one flat list.

Both products agree on the key boundary: **you do not share one deployment across two workspaces.**

### Access

| Topic | Astro | Flowdeck |
|-------|------|----------|
| Narrow access within a workspace | **Workspace Accessor** role + deployment-level roles. Assigning a deployment role automatically grants Accessor. | **Project** roles or deployment roles — no separate Accessor concept needed. |
| Teams / groups | Teams apply a shared workspace role to a group of users. | Directory groups map to roles at any level via SSO. |
| CI/CD enforcement | Set as a workspace default; can be overridden per deployment. | Set at workspace level; applies to all deployments in that workspace. |
| Custom roles | Custom deployment roles defined at org level, optionally restricted to specific workspaces. | Fixed named roles per scope in this version. |
| DAG-level access | Supported on recent Astro Runtime versions. | Not in scope for this guide; deployment is the smallest structural unit here. |

### The main difference

In Astro, teams that want "project-style" isolation typically create more workspaces. In Flowdeck, teams create more projects inside a workspace. The trade-off: Astro's approach gives stronger isolation by default; Flowdeck's approach keeps shared workspace settings (alerts, guardrails, SSO) without multiplying workspaces.

---

## Closing note

The four levels are not organisational bureaucracy — they are decisions. **Organisation** decides billing and company-wide rules. **Workspace** decides which teams are independent of each other. **Project** decides which programmes share settings and narrow access. **Deployment** is where work actually runs.

Get the workspace boundaries right first. Everything else — who can see what, how code reaches production, how teams are onboarded — follows from that.
